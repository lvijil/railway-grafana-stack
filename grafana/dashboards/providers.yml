apiVersion: 1

providers:
  - name: ViaTrack
    orgId: 1
    folder: ViaTrack
    folderUid: viatrack
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 300
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
